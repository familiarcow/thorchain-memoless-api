# THORChain Memoless API Configuration

# ===========================================
# DEVELOPMENT TIPS
# ===========================================
# 1. Copy this file to .env and update the values
# 2. Use stagenet for development (safer and cheaper)
# 4. For PostgreSQL, create the database first:
#    createdb thorchain_memoless
# 5. The API will create tables automatically on first run

# ===========================================
# REQUIRED ENVIRONMENT VARIABLES
# ===========================================

# Hot Wallet Configuration (REQUIRED)
# Make sure it is funded with enough RUNE to register your transactions
HOT_WALLET_MNEMONIC="abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"

# Database Configuration (OPTIONAL - will run without persistent storage if not provided)
# For development, you can use PostgreSQL or SQLite
# PostgreSQL example:
# DATABASE_URL="postgresql://username:password@localhost:5432/thorchain_memoless"
# SQLite example (easier for development):
DATABASE_URL="sqlite:./dev-database.db"

# ===========================================
# NETWORK CONFIGURATION
# ===========================================

# THORChain Network (mainnet or stagenet)
THORCHAIN_NETWORK=stagenet

# API Server Configuration
# Set NODE_ENV to development for verbose logs
# Set NODE_ENV to production for less logs
NODE_ENV=development
PORT=8080

# ===========================================
# OPTIONAL CONFIGURATION
# ===========================================

# CORS Configuration
# Make sure your PORT from above is whitelisted in CORS

CORS_ORIGINS=http://localhost:8080

# Rate Limiting
RATE_LIMIT=100

# Logging
LOG_LEVEL=info

# ===========================================
# NOTIFICATION WEBHOOKS
# ===========================================

# Enable/disable webhook notifications
ENABLE_DISCORD_WEBHOOK=false
ENABLE_SLACK_WEBHOOK=false

# Discord webhook URL for notifications
# DISCORD_WEBHOOK=https://discord.com/api/webhooks/YOUR/WEBHOOK/URL

# Slack webhook URL for notifications (optional)
# SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Low Balance Alert Configuration
# Minimum registrations remaining before sending low balance alert
# Requires Discord or Slack Webhook Enabled
LOW_BALANCE_ALERT_THRESHOLD=25

# ===========================================
# AFFILIATE CONFIGURATION
# ===========================================

# Enable/disable automatic affiliate injection in swap and liquidity memos
# Your below address & fee will be automatically injected when it detects a swap. No changes to any other memos
INJECT_AFFILIATE_IN_SWAPS=true

# THORName or address for affiliate fees
# Examples: "wd", "dx", "t", "thor1234...", etc. Leave empty to disable affiliate fees
AFFILIATE_THORNAME=yourTHORNAMEorTHORaddressHERE

# Affiliate fee in basis points (1 basis point = 0.01%, max 10000 = 100%)
# Example: 5 = 0.05%, 50 = 0.5%, 500 = 5%
AFFILIATE_FEE_BP=5